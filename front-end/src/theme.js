import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    background: {
      default: '#fafafa',
    },
    primary: {
      main: '#212121',
    },
    secondary: {
      main: '#9e9e9e',
    },
  },
});

export default theme;
